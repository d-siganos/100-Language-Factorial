#include <stdio.h>
int main()
{
   int number;
   printf("Enter a number: ");
   fgets(number,20,stdin);
   result = factorial()
   return 0;
}

int max(int number) {
   int result;
 
   if (num1 > num2)
      result = num1;
   else
      result = num2;
 
   return result; 
}
